# Mermaid (Flowchart / Graph) 作成ルール

Mermaidで図を作成・編集する際は、構文エラーおよび画面での見切り（はみ出し）を防ぐために以下の表記ルールを厳格に遵守してください。

## 1. ノード表記およびテキスト改行ルール（最重要）
* **特殊文字・記号が含まれるテキストは必ずダブルクォーテーションで囲む**
  ノードの表示テキスト内に **半角括弧 `()`**、**角括弧 `[]`**、**カンマ `,`**、**コロン `:`** などの記号やスペースが含まれる場合は、必ず `["..."]` の形式で記述してください。
  * ❌ 非推奨: `A[Market Data (Yahoo)]`
  * ⭕ 推奨: `A["Market Data (Yahoo)"]`

* **全角12文字を超える場合は `<br/>` で改行する**
  1行のテキスト長が**全角12文字（半角相当24文字）**を超える場合、表示が見切れてしまうため、**意味の切れ目で `<br/>` タグを使って改行**してください。
  * ❌ 非推奨: `A["Command Line Interface"]` （長すぎて見切れる）
  * ⭕ 推奨: `A["Command Line<br/>Interface"]`
  * ⭕ 推奨: `B["ローカルデータストア<br/>(SQLite / Parquet)"]`

## 2. Subgraph（サブグラフ）の定義
* **IDと表示タイトルを分離する**
  `subgraph` の識別子（ID）にはスペースや日本語・記号を使用せず、画面表示用のタイトルは角カッコ `[ ]` 内に別途記述してください。
  * ❌ 非推奨: `subgraph Data Layer`
  * ⭕ 推奨: `subgraph Data_Layer [Data Layer]`

## 3. ノードID / Subgraph ID の命名規則
* **IDに使用できる文字**
  ノードIDおよびサブグラフIDは **半角英数字** と **アンダースコア `_`** のみで構成してください。
  * 数字から始めない（例: `Node_1` とする）。
  * 日本語や特殊文字はIDに直接使わず、表示テキスト `["..."]` 側に記述してください。
  * ⭕ 例: `User_DB["ユーザー<br/>データベース"]`

## 4. 矢印・エッジラベルの記法
* **ラベル内の記号保護**
  矢印（エッジ）の上に表示するテキストに括弧や記号が含まれる場合も、ダブルクォーテーションで囲んでください。
  * ❌ 非推奨: `A -- GUI (Future) --> B`
  * ⭕ 推奨: `A -- "GUI (Future)" --> B`

## 5. 出力フォーマット
* コードブロックは ```mermaid で開始してください。
* 先頭行には必ず方向指定（`graph TD` や `flowchart LR` など）を明記してください。